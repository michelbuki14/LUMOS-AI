name: lumos_mcp
description: LUMOS AI — MCP (Model Context Protocol) client/server — permission-aware, replaceable.
version: 0.1.0
environment:
  sdk: '>=3.5.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  web_socket_channel: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  lints: ^5.0.0
