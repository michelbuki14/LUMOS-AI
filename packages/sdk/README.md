# LUMOS SDK — Stable Plugin SDK

Semver. Apache 2.0.

## Layout

```
packages/sdk/
├── lib/lumos_sdk.dart
├── lib/src/plugins.dart
└── README.md
```

See `docs/sdk-guide.md` + `docs/plugin-guide.md`.

## Versioning

`MAJOR.MINOR.PATCH` — breaking changes require major + `MIGRATION.md`.
