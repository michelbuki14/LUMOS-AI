name: lumos_sdk
description: LUMOS SDK — stable plugin + agent surface, semver, Apache-2.0.
version: 0.1.0
environment:
  sdk: '>=3.5.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
